version: '3.8'
services:
  db:
    image: gvenzl/oracle-free:latest
    container_name: figureshop-oracle
    environment:
      ORACLE_PASSWORD: 'P@ssw0rd123'      # For SYS/SYSTEM
      APP_USER: 'figureshop'              # Creates this user automatically!
      APP_USER_PASSWORD: 'P@ssw0rd123'    # Password for the app user
    ports:
      - '1521:1521'
    volumes:
      - oracle-data:/opt/oracle/oradata
volumes:
  oracle-data: